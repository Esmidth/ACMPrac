#include "stdafx.h"
#include <iostream>

using namespace std;

int main()
{
	int ti;
	cin >> ti;
	int numberofRoom;
	cin >> numberofRoom;
	int x, y;
	int killP[10000] = { '\0' };
	int Ei[10000] = { '\0' };
	for (int i = 0; i < numberofRoom - 1;i++)
	{
		cin >> x >> y;

	}
	for (int i = 0; i < numberofRoom;i++)
	{
		cin >> killP[i] >> Ei[i];
	}
}